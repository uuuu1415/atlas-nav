# Contributing to Atlas Nav

感谢你愿意改进 Atlas Nav。贡献说明是一份给未来贡献者的协作约定：它说明项目欢迎什么类型的改动、如何在本地运行、提交代码前要检查什么，以及如何提出 Issue 或 Pull Request。它不是使用文档，而是让他人能以一致方式参与维护的指南。

## 开始前

请先搜索已有的 Issue，避免重复问题。若计划新增功能或大范围改动，先创建 Issue 说明使用场景、方案和影响范围，得到讨论后再开始开发。

本地需要 Node.js 22.5 或更新版本。复制 `.env.example` 为 `.env`，设置自己的 `ADMIN_PASSWORD` 与 `SESSION_SECRET`，再执行 `npm install` 和 `npm run dev`。不要提交 `.env`、`data/`、`storage/` 或真实的备份文件。

## 开发原则

保持公共首页轻量，后台改动必须有服务端鉴权和输入校验。新的设置项应通过数据层保存，避免将用户可调整内容写死到页面；对视觉设置使用受控令牌，不接受任意 CSS。任何会请求外部 URL 的功能都必须遵守现有的内网地址和重定向安全检查。

请优先编写可读、命名明确的函数，并在安全边界、迁移和不直观的业务规则处添加简短注释。不要重新引入大段压缩的一行式脚本。

## 提交 Pull Request

每个 Pull Request 聚焦一个问题。描述中应说明改动动机、用户可见变化、验证方式，以及是否修改了数据库结构或环境变量。提交前请完成相关流程，确认浏览器控制台没有红色错误。

对于界面变化，请附上首页或后台截图；对于安全、数据或部署变化，请补充 README、ARCHITECTURE.md 或 TESTING.md 的对应说明。

## 行为准则

保持讨论具体、尊重和以解决问题为导向。我们欢迎报告 Bug、改进文档、翻译、设计建议和代码贡献。
