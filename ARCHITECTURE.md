# 代码与配置说明

## 代码结构

公共首页已经从单个压缩脚本拆分为 ES Module：`public/js/shared.js` 负责 DOM、转义与语言词条，`public/js/home.js` 负责首页状态、搜索、布局、最近访问和页面渲染。这样首页交互不再依赖一个难以维护的单行文件。

服务端保留单一入口 `server.js`，但安全校验、请求包装、设计令牌、数据校验和路由分别以命名函数组织。SQLite 数据读写仍集中于 `lib/sqlite.js`，避免 SQL 逻辑散落到路由中。

## 后台设计系统

在后台点击“站点设置”，新的“视觉令牌”区域可调整页面背景、正文文字、弱化文字、卡片背景、描边和强调色，以及内容最大宽度和区块间距。服务端会对颜色及尺寸格式进行白名单校验，仅允许 `#RRGGBB`、`px`、`rem` 或 `%`，避免任意 CSS 被写入数据库。

首页读取这些令牌后通过 CSS 自定义属性应用。默认值在服务端集中定义，已有站点数据库会在下一次服务启动时自动补充缺失设置。

## 维护准则

链接、分类、搜索引擎、页面文案、布局和视觉令牌都优先由后台数据驱动；仅有安全限制、交互结构和响应式规则保留在代码中。新增可配置项时，应先添加服务器端校验，再把安全值存到 `settings` 表，最后在前端以 CSS 变量或受控文本呈现。
